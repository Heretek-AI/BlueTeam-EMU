# BlueTeam-EMU local configuration. Copy to `.env`.

# Display name used for the auto-created local user on first run.
BLUETEAM_EMU_USER=analyst

# Path to the SQLite database file. Defaults to ./data/app.db
DATABASE_URL=./data/app.db
